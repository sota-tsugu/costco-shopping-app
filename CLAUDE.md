# CLAUDE.md

このファイルは、新しいセッションのClaude Coworkが最初に読んで経緯を把握するためのものです。

## 開発者について(最重要の前提)

- 開発者(SOTAさん)は非エンジニアで、プログラミングの専門知識はほとんどない
- コードは基本的にClaude Coworkが書き、SOTAさんは「一緒に伴走してもらう」立場
- **無料運用が絶対条件**。有料プラン・従量課金が発生する提案はしない。無料枠を超える可能性がある場合は必ず事前に明示する
- 専門用語には簡単な補足を付ける(例:「Web Worker(画面の動きを止めずに裏側で計算処理を行う仕組み)」)
- ライブラリ選定など判断が分かれる場面では、勝手に決めず理由と選択肢を示す
- **判断に迷ったら推測で進めず、必ず質問する**
- 開発端末は自宅のMacBook Pro(特別な指示がなければこれを前提とする)

## このアプリについて

我が家専用のコストコ買い物リストアプリ。2026年8月に企画・実装を大きく白紙に戻し(詳細は次の節)、SOTAさんとゼロベースで話し合った結果を [`costco_app_concept_v3.md`](./costco_app_concept_v3.md) にまとめてある。**新しいセッションではまずこのv3を読むこと。** 初期の企画コンセプト(v2)は [`costco_app_concept_v2.md`](./costco_app_concept_v2.md) に参考として残っているが、内容はv3に置き換わっている。

v3はあくまで方向性の合意メモであり、データ構造・画面遷移などの細部はまだ決まっていない(v3の「4. 未確定・今後決めること」を参照)。実装を始める前に、その時点で残っている未確定事項をSOTAさんと相談すること。

## 白紙化(2026年8月)

STEP0(環境構築)→ STEP1(フェーズ1-a)→ フェーズ1-b → フェーズ2(パートナーとのリアルタイム共有、Firebase移行)まで一通り実装が進んでいたが、SOTAさんから「環境構築以外は白紙に戻して、アプリについてゼロベースから検討し直したい」との要望があり、実装を大きく削除した。

**残したもの**(SOTAさんとの相談の上で決定):
- STEP0で作った開発環境の土台(Vite + React + TypeScript、Tailwind CSS / Lucide / Zustand の導入、PWA設定、GitHub Actionsでの自動デプロイ)
- フェーズ2で作ったFirebase基盤(匿名認証 + 家族コードによる、パートナーとのデータ共有の仕組み)。理由:非エンジニアのSOTAさんにとって認証・共有の仕組みはゼロから作り直すコストが高く、どんなアプリになってもこの基盤自体は概ね再利用できると判断したため
- 商品名の入力候補データベース(`src/data/productCatalog.ts`。costcotuu.comの商品一覧、約3200件)。実害なく再利用できる参考データのため
- カートの画像素材(`src/assets/cart-icon.jpg`、リポジトリ直下の`カートイメージ1〜3`)。SOTAさん提供の画像で、今後の演出などに再利用できるため

**削除したもの**:
- STEP1・フェーズ1-bで作った画面・機能一式:予算設定画面(BudgetSetupScreen)、買い物画面(ShoppingScreen)、マイ定番棚の管理・編集画面、商品追加フォーム、購入履歴・価格比較画面、事前買い物予定リスト、カートの中身確認画面など
- それらを支えていたデータの持ち方(`src/store/cartStore.ts`。Product/ShoppingTrip/Purchase/Wishlistというオブジェクト設計そのもの)
- 旧sql.js実装一式(`src/db/`以下)と、それをFirestoreへ引き継ぐための一度きりの移行ツール(`src/firebase/migrateLocalData.ts`)。対象データ(旧cartStoreの仕組み)自体がなくなったため不要になった
- 価格比較の計算ロジック(`src/utils/priceCompare.ts`)、値上がり/値下がりバッジ(`src/components/PriceDiffBadge.tsx`)
- `package.json`の`sql.js`/`@types/sql.js`(旧sql.js実装の削除に伴い不要になったため)

**今、実際に動く画面**:
- `HouseholdSetupScreen.tsx`:初回起動時の「家族を作る/参加する」画面(そのまま維持)
- `App.tsx`:家族コード設定後は、まだ何もないことが分かる簡単なプレースホルダー画面のみ(ヘッダーに設定アイコンあり)
- `SettingsModal.tsx`:アプリの強制更新・家族コードの確認/切り替え(そのまま維持)

**注意**:これまでの実装は全てGitのコミット履歴に残っているため、完全に失われたわけではない。「あの機能を復活させたい」となった場合は、コミットログから該当ファイルを復元できる。`costco_app_concept_v2.md`(初期企画書)も削除せず参照用に残しているが、内容は現状と一致しない箇所が多いことに注意。

### 過去の実装の要点(参考・アーカイブ)

詳しい実装内容は上記の通りGit履歴を参照すればよいが、意思決定の経緯として知っておくと良い点だけ簡単に記す。

- 買い物の一連の流れは「予算設定 → 定番棚からカートに追加 → 会計完了」という最小ループから始め、単価比較・購入履歴・事前リストなどを段階的に追加していった
- 途中で「パートナーと共有したい」という要望が出たことがきっかけで、データの持ち方をブラウザ内のみ(sql.js)からFirebase Firestore(クラウド)へ全面移行した。この移行の経緯・設計判断(家族コード方式、セキュリティ上の割り切りなど)は下の「Firebase基盤」節に引き継いで記載している
- 後半では、UIをコストコのブランドカラー(赤・青・白のトリコロール)に配色し直したり、カート追加時に価格・内容量をその場で確認する仕組みや、購入記録を1件ずつ訂正できる機能など、オブジェクト指向UI(OOUI)的な改善も進めていた

## Firebase基盤

白紙化後も残しているFirebaseまわりの仕組み。今後どんなアプリを作るとしても、土台として使う想定。

### 認証・共有の仕組み(「家族コード」方式)

- 各自のGoogleアカウントでログインする方式ではなく、**匿名認証(Firebase Authentication)+ 家族コード(合言葉のようなランダムな文字列。例: `K3F9-7QXP-2MRT`)** を家族内で共有する方式を採用(`src/firebase/household.ts`)。SOTAさんが以前使ったFirebaseアプリでも個人アカウントログインはしていなかった、という経緯を踏まえた選定
- 家族コードは端末のlocalStorageに保存する。データはFirestoreの `households/{householdId}/...` 以下に保存され、その家族コードを知っている端末だけが読み書きできる
- **セキュリティ上の割り切り**:Firestoreのセキュリティルール(`firestore.rules`)は「サインイン済み(匿名でも可)」だけを要求しており、「本当にその家族のメンバーか」までは厳密に確認していない。家族コードという文字列を知っているかどうかだけがアクセス制御になっている(合言葉方式)。Cloud Functions(サーバー側処理)を使えばもっと厳密にできるが、無料の Spark プランの制約上、また非エンジニアが保守する前提を踏まえ、あえてシンプルな方式にしている。今後扱うデータの重要度によっては見直しが必要になる可能性がある
- 初回起動時、`HouseholdSetupScreen.tsx` で「新しく家族を作る」か「家族コードを入力して参加する」かを選ぶ

### オフライン対応

- Firestoreの`persistentLocalCache`(`src/firebase/config.ts`)により、オフラインでもデータの読み書きができ、オンラインに戻ると自動で同期される(Firebase SDKの標準機能)

### Firebaseプロジェクト情報

- プロジェクトID:`costco-shopping-app-39395`
- 無料の Spark プラン(従量課金のBlazeプランへは意図的にアップグレードしていない)
- `src/firebase/config.ts`内の`firebaseConfig`(apiKeyなど)はコード内に直接書いているが、これはFirebase Webアプリでは意図された公開情報であり、通常の「秘匿情報をコードに書かない」ルールの対象外(実際のアクセス制御は`firestore.rules`で行う)
- `firestore.rules`はこのリポジトリで管理しているが、Firebase CLIでのデプロイは行っていない(Claude Coworkの作業環境がネット接続不可のため)。**内容を変更した場合は、SOTAさんにFirebaseコンソール→Firestore Database→「ルール」タブに手動で貼り付けてもらう必要がある**
- 現在Firestoreには具体的なコレクション(データの入れ物)は設計していない。新しい企画が決まった段階で、あらためて`households/{householdId}/...`配下にどんなコレクションを置くか設計する

## 技術構成(白紙化後、2026年8月時点)

- フロントエンド:React (Vite) + TypeScript
- データ保存:Firebase Firestore(基盤のみ残っており、具体的なデータ設計は未定。上の「Firebase基盤」節を参照)
- 認証:Firebase Authentication の匿名認証 + 家族コード(合言葉)方式
- スタイリング:Tailwind CSS(コストコのブランドカラーに近いカスタムカラー`costco-red`/`costco-blue`を`tailwind.config.js`に定義済み。デザイン資産として維持)
- アイコン:Lucide
- 状態管理:Zustand(パッケージは入っているが、白紙化により現在は実際の使用箇所なし)
- PWA対応:`vite-plugin-pwa`によるService Workerでオフラインキャッシュ
- ホスティング:GitHub Pages(リポジトリ名 `costco-shopping-app` を前提にvite.config.tsの`base`を設定済み。リポジトリ名を変える場合はここも変更が必要)
- バージョン管理:GitHub

## STEP0で行った技術的な判断(理由付き)

- **sql.jsのWASMファイルはViteの`?url`インポートでバンドルする構成にしていた**(現在はsql.js自体を削除済みだが、同様の手法は他のアセットでも使える考え方として記録):postinstallスクリプトでのファイルコピーより構成がシンプルで、PWAのキャッシュ対象にも自動的に含まれるため

## 【重要】Claude Coworkの作業環境に関する制約

STEP0の作業中に判明した、今後のセッションでも影響する制約です。

- **Claude Coworkのシェル(bashツール)経由の作業環境は、インターネットに接続できません**(GitHub・npmレジストリなど外部サイトへの接続がすべて遮断される)。これは個人アカウントでは変更できない標準の制限
- そのため、Claude自身が`npm install`を実行したり、`git push`でGitHubに直接コードを送ったりすることはできない
- **回避策**:
  - コードファイルの作成・編集、ローカルでのgitコミット(`git init`/`add`/`commit`)はネットワーク不要なので問題なく行える
  - GitHubへの実際の送信(push)は、**GitHub Desktop(無料アプリ)をSOTAさんが操作して行う**運用にした。Claudeが変更をコミットしたら、SOTAさんにGitHub Desktopで「Push origin」ボタンを押してもらう、という流れになる
  - `npm install`や`npm run build`が実際に動くかどうかの確認は、ローカルではなく**GitHub Actions(GitHubのサーバー上で実行されるためネット接続あり)の実行結果で確認する**。そのため、pushしてからActionsのログを見る、というサイクルで動作確認を行う点に注意
- 今後、上記の制約が解除された(ネットワークが使えるようになった)場合は、この節を更新すること

## リポジトリ・公開設定

- GitHubユーザー名:`sota-tsugu`
- リポジトリ名:`costco-shopping-app`
- 公開URL:`https://sota-tsugu.github.io/costco-shopping-app/`
- リポジトリはPublic(公開)設定(無料でGitHub Pagesを使うための制約)

## セキュリティ・秘匿情報の扱い

- APIキー・パスワードなどはコードに直接書き込まない(**例外:Firebaseの`firebaseConfig`は公開情報のため直書きしている。詳細は「Firebase基盤」節を参照**)
- 将来必要になった場合は`.env`ファイル(`.gitignore`で除外済み)で管理する
- GitHubの個人アクセストークンなど、認証情報はコード・リポジトリ内に一切保存しない
