# STEP0:環境構築とプロジェクト初期化

## 開発者について(重要な前提)

この指示書を出している私は非エンジニアで、プログラミングの専門知識はほとんどありません。コードを自分で書くのではなく、Claude Coworkに伴走してもらいながら、一緒にこのアプリを作っていきたいと考えています。作業を進める際は、以下を徹底してください。

- 専門用語を使う場合は、簡単な補足を添えてください(例:「Web Worker(画面の動きを止めずに裏側で計算処理を行う仕組み)」のように)
- 何か判断が分かれる場面(ライブラリの選定、設定の細部など)では、勝手に高度な方を選ばず、理由と一緒に選択肢を示してください
- **無料であることを最優先の制約としてください**。有料プラン・従量課金が発生するサービス・ツールは、たとえ便利であっても提案しないでください。もし無料枠を超える可能性がある選択をする場合は、必ず事前に明示してください
- 作業の節目ごとに「今何をしたか」「次に何をするか」を分かりやすく説明してください。専門的な実装の詳細より、全体像の把握を優先してください
- このプロジェクトは今後も長期的にClaude Coworkと一緒に進めていく前提のため、コードやドキュメントは「後から別のセッションのClaudeが読んでも分かる」状態を意識してください

**開発端末について**
今回の開発は自宅のMacBook Proで行います。今後、職場のMacBook Airなど別の端末で作業を再開する可能性もありますが、その場合は改めてその旨を伝えます。特別な指示がない限り、この端末での作業を前提に進めてください。

**詰まった時のルール**
判断に迷う場面や、想定通りに動かない場面があれば、推測で進めず必ず立ち止まって質問してください。非エンジニアの私は、間違ったまま進んでしまっても気づきにくいため、不確実なまま先に進むより、都度確認を取ることを優先してください。

---

## このSTEPの目的

「我が家専用コストコ買い物リストアプリ」開発の最初の一歩として、開発環境とプロジェクトの土台だけを作る。この段階では機能は一切実装しない。次のSTEP1(フェーズ1-a実装)にスムーズに進めるための準備だけを行う。

このSTEPが終わった時点のゴールは「空っぽだが、正しく起動してGitHub Pagesに公開できる状態のアプリ」ができていること。

---

## 前提となる技術構成(決定済み・変更しない)

- フロントエンド:React (Vite) + TypeScript
- データ保存:sql.js(ブラウザ内SQLite/WASM)を **Web Worker内**で動かす構成。メインスレッドで直接sql.jsを呼び出さないこと
- 永続化:sql.jsはメモリ上で動くDBのため、変更のたびにIndexedDBへシリアライズして保存する処理が必要
- スタイリング:Tailwind CSS
- アイコン:Lucide
- 状態管理:Zustand
- PWA対応:Service Workerでオフラインキャッシュ
- ホスティング:GitHub Pages
- バージョン管理:GitHub(GitHub Actionsで自動デプロイ)

---

## やってほしいこと(タスク一覧)

### 0. 事前準備の確認
作業を始める前に、以下がこのMacBook Proで揃っているかを確認してください。足りないものがあれば、インストール手順も含めて案内してください。
- Node.jsがインストールされているか(バージョンも確認)
- Gitが使える状態か
- GitHubアカウントを持っているか(なければ作成の案内をしてください)

### 1. リポジトリとプロジェクトの初期化
- 新しいGitHubリポジトリを作成する(リポジトリ名の提案もお願いします)
- Vite + React + TypeScriptのテンプレートでプロジェクトを初期化する
- Tailwind CSSをセットアップする
- Lucide, Zustandを依存関係に追加する

### 2. sql.jsをWeb Worker上で動かす土台を作る
- sql.jsをWeb Worker内で初期化するための最小限のセットアップを行う
- メインスレッドとWorker間のやり取りは、シンプルに実装できる方法(Comlinkなどの利用も検討可)で構築する
- 「Workerが起動し、簡単なSQL(例:`SELECT 1`)が実行できて結果が返ってくる」ことを確認できる状態にする

### 3. データの永続化(IndexedDB)の土台を作る
- sql.js上のデータをIndexedDBへ保存・読み込みできる最小限の仕組みを作る
- ブラウザをリロードしても、直前の状態が復元されることを確認できるようにする(この時点ではテスト用のダミーデータで良い)

### 4. PWA設定
- manifest.jsonを用意し、ホーム画面に追加できる状態にする
- Service Workerでアプリ本体とsql.jsのWASMファイルをキャッシュし、オフラインでも起動できるようにする

### 5. GitHub Pagesへのデプロイ設定
- GitHub Actionsを使い、mainブランチへのプッシュで自動的にGitHub Pagesへデプロイされる仕組みを作る
- 実際にデプロイして、公開URLでアプリが表示されることを確認する

### 6. プロジェクトの基本ドキュメントを残す
- README.mdに、このアプリの目的・技術構成・開発の進め方(フェーズ1-a〜フェーズ4がある旨)を簡潔にまとめる
- 非エンジニアの自分が半年後に見ても分かるように、専門用語には簡単な補足を入れてほしい

### 7. CLAUDE.mdファイルの作成
プロジェクトルートに`CLAUDE.md`を作成し、新しいセッションのClaude Coworkが最初に読めば経緯を把握できるようにしてください。以下を含めてください。
- 開発者が非エンジニアであること、無料運用が絶対条件であること
- 技術構成の概要と、それを選んだ理由
- 現在どのフェーズまで進んでいるか(このSTEP0時点では「環境構築中」)
- 判断に迷ったら推測で進めず、必ず質問すること

### 8. セキュリティの基本ルール(念のため)
- APIキー、パスワードなどの秘匿情報は、コード内に直接書き込まないでください
- 将来こうした情報が必要になった場合は、`.env`ファイルなど、GitHubに公開されない方法で管理する方法を案内してください(この段階では該当する情報はない想定です)

---

## このSTEPでやらないこと(念のため明記)

- 商品の登録・カート機能などの実際のアプリ機能(次のSTEP1以降で実装)
- Product / Purchase / ShoppingTripなどの本番用テーブル設計の実装(この段階ではダミーテーブルで動作確認できれば十分)
- デザインの作り込み(この段階では見た目は簡素で良い)

---

## 完了の確認基準(このSTEPが終わったと言える状態)

- [ ] GitHubリポジトリが作成され、コードがプッシュされている
- [ ] `npm run dev`でローカル環境が問題なく起動する
- [ ] Web Worker経由でsql.jsのクエリが実行でき、結果が画面に表示できる(簡単な確認表示でよい)
- [ ] リロードしてもIndexedDBに保存したダミーデータが復元される
- [ ] GitHub Pagesの公開URLでアプリが表示され、スマートフォンのホーム画面に追加できる
- [ ] オフライン(機内モードなど)でも再度アプリが起動する
- [ ] README.mdにプロジェクト概要が書かれている
- [ ] CLAUDE.mdが作成され、プロジェクトの前提が記録されている
- [ ] ここまでの作業で有料プラン・従量課金が発生するサービスを使っていないことが確認できている

---

## 次のSTEPについて

このSTEP0が完了したら、STEP1として「フェーズ1-a(合計金額のリアルタイム表示・予算進捗・マイ定番棚・片手操作UI)」の実装に進みます。STEP1の指示書は、STEP0の完了を確認してから別途作成します。
