# 我が家専用コストコ買い物リストアプリ 開発コンセプト・方針・手順 v2

## 0. 一枚要約

コストコ店内でカートを押しながら片手で使う、我が家専用のオフライン買い物アプリ。合計金額と予算進捗を常時表示し、入力はタップ中心でほぼゼロにする。実際の購入パターンは「ほぼ決まったものを買う」ため、他の買い物アプリにはない核は「自動リスト生成」(購入履歴と周期から次回のリストをアプリ自身が下書きする)。技術はReact+sql.js(SQLite/WASM)をWeb Workerで動かすPWA構成とし、GitHub Pagesで無料ホスティングする。機能を広げる前に、まず「合計金額+予算+定番棚」だけの最小ループを2週間以内に実際の店内で使える状態にすることを最優先とする。

## 0.5 やらないこと(非ゴール)

スコープが際限なく広がらないよう、明確に対象外とする。
- 不特定多数への公開・アプリストア配信(あくまで家族専用)
- コストコ以外の店舗・チェーンへの汎用対応
- 他ECサイト・価格比較サイトとの連携
- 多言語対応
- 広告や課金による収益化
- 初期段階でのリアルタイム家族間同期(将来の拡張候補としては残すが、今は着手しない)

これらを対象外と決めることで、「迷いログ」「片手操作」「単価比較」という核の体験を磨くことにリソースを集中する。

---

## 1. コンセプト

**アプリ名(仮)**: 我が家専用コストコ買い物リストアプリ

**目的**: 不特定多数向けではなく、自分と家族が実際にコストコの店内で使うことに特化した、無料で作成・無料で使えるアプリ。店内でのストレス(片手操作・入力の手間・電波不安定)を解消しつつ、予算管理と単価比較をリアルタイムで行えるようにする。

**このアプリならではの価値**
このアプリは単なる「買い物リスト+家計簿」ではなく、コストコというブランド・店舗特性(大容量商品、会員制、セール品の入れ替わりの早さ、片手操作前提の巨大カート)に最適化された専用設計である点が独自性。実際の購入行動を振り返ると、コストコでは毎回ほぼ決まったものを買うことが多く、迷うことは少ない。そのため核となる差別化機能は「自動リスト生成」とする:過去の購入履歴と購入周期から、次回のトリップ前にアプリ自身がリストの下書きを用意し、ユーザーは不要なものを外すだけで済むようにする。「入力を減らす」のではなく「入力自体をなくす」ことを目指す機能であり、ルーティン購入が中心という実際の使い方に最も噛み合う。
「迷いログ」(買った/迷った+理由を記録し、次回来店時に想起させる)は、たまに初めての商品で迷う場面向けのオプション機能として引き続き用意するが、アプリの核ではなく補助的な位置づけとする。

**なぜ使い続けたくなるか(継続の仕組み)**

機能を並べるだけでなく、「次のコストコでも自然と開きたくなる」流れを意図的に設計する。

1. **トリガー**:次回コストコ予定日の近さ、または自動生成されたリストの下書きが用意されたという通知が、開くきっかけになる
2. **行動**:自動生成されたリストから不要なものを外すだけ、足りないものだけタップで追加。行動のハードルを極小化する
3. **報酬**:リストがほぼ何もせずに完成している楽さ、単価が下がっている発見、戦利品画面での達成感
4. **蓄積(投資)**:使うほど購入周期の予測精度が上がり、次回のリスト下書きがより正確になる。この「使うほど自分専用に育つ」感覚が離脱を防ぐ核になる

---

## 2. 開発方針

- 非エンジニアでも保守できるシンプルな設計にする
- 完全無料で作成・運用する(サーバー費用ゼロを維持)
- サーバーを極力使わず、オフラインファーストで設計する
- Claude(Cowork)が継続して開発・保守しやすい構成にする
- 機能の数よりも「使いやすさ」「入力の少なさ」を最優先する
- 店内では完璧な入力を求めず、後から直せる2段階方式(店内は最小限の記録→帰宅後に整える)を許容する

---

## 3. 家族内の温度差を踏まえたペルソナ設計

開発者本人と家族とでは、アプリへの期待値が異なる可能性が高い。この差を無視すると「自分だけが使うアプリ」になりかねない。

- **開発者本人**:全機能を使いこなしたい。単価比較や迷いログなど分析的な機能も歓迎
- **家族(配偶者など)**:シンプルに「今いくらか」「何を買うか」が分かればよく、複雑な機能は負担に感じる可能性がある

**対策**:設定で「シンプルモード/パワーユーザーモード」を切り替えられるようにする。シンプルモードでは合計金額・予算・カート追加のみを表示し、単価比較や迷いログなどの分析系機能は非表示にする。初期段階では開発者本人が入力担当になっても運用が回る設計にしておき、家族の参加は段階的に促す。

---

## 4. 技術構成(決定)

Gemini案(候補A:React+LocalStorage)、ChatGPT案(候補B:Flutter+SQLite)を比較検討した上で、両者の利点を両立する**候補C**を最終構成として採用する。

| 項目 | 候補A(Gemini) | 候補B(ChatGPT) | **候補C(採用)** |
|---|---|---|---|
| フロントエンド | React (Vite) + Tailwind + Lucide | Flutter | **React (Vite) + TypeScript** |
| データ保存 | LocalStorage | SQLite | **sql.js(ブラウザ内SQLite/WASM)+ IndexedDBで永続化** |
| 実行環境 | Webブラウザ(PWA) | ネイティブアプリ寄り | **Webブラウザ(PWA)** |
| データ構造の堅牢さ | 弱い | 強い | **強い(候補Bと同等のSQLテーブル設計をそのまま実装)** |
| 配布方法 | URL共有 | ビルド・ストア配布(手間大) | **URL共有(GitHub Pagesでホスティング)** |
| バックアップ | GAS→スプレッドシート連携の余地 | SQLiteファイル書き出し | **.sqliteファイルの書き出し** |

**採用理由**: Flutter(候補B)はビルド環境(Xcode/Android Studio)の維持が非エンジニアには負担が大きく、開発が止まるリスクがある。LocalStorageのみ(候補A)はデータ量増加時の検索・集計性能に弱い。候補Cはこの両方の弱点を回避しつつ、ChatGPT案の優れたデータ設計(Product/Purchase/ShoppingTripのリレーショナル構造)をそのままSQLとして実装できる。

**周辺ツール**
- **GitHub**: 採用する。コードのバージョン管理と、GitHub Pagesでの無料ホスティングを兼ねる。Claude Coworkでの修正→GitHubへのプッシュ→自動デプロイ(GitHub Actions)というシンプルな運用サイクルを組む
- **Firebase**: 現時点では不採用。「サーバーを極力使わない・オフラインファースト」という方針と矛盾するため。将来「家族間リアルタイム共有」機能が本当に必要になった段階で、拡張レイヤーとして再検討する

**パフォーマンス面の工夫(重要な修正点)**
- **sql.jsはWeb Worker内で動かす**:sql.jsのクエリ実行は同期処理のため、メインスレッドで直接動かすとタップ操作の反応が一瞬止まる(カクつく)原因になる。Web Workerに隔離し、Comlinkのような軽量ライブラリでやり取りすることで、UIの操作性を犠牲にしない
- **sql.jsはメモリ上で動くDBである点に注意**:ブラウザを閉じると消えるため、操作のたびに変更をIndexedDBへシリアライズして明示的に永続化する処理が必要(候補Bの「SQLiteだから自動的に永続化される」という前提とは異なる、Web版特有の注意点)
- 起動直後はキャッシュ済みのローカルデータを先に表示し、sql.jsの初期化を裏側で進める(体感速度優先)
- Service Workerでアプリ本体・DBエンジンを事前キャッシュし、2回目以降の起動を高速化
- カート内の合計金額計算はメモリ上(Zustandなどの軽量な状態管理)で即時実行し、SQLite側への書き込みは非同期でバックグラウンド実行(タップ操作を待たせない)
- 開発の継続性を保つため、CI(GitHub Actions)で最低限のスモークテスト(アプリが起動しカートに追加できるか程度)を自動チェックする仕組みも検討する。ハードルが高ければ初期は見送って良い

---

## 5. データ設計(方針として確定・v2で拡張)

技術スタックによらず、以下のデータ設計方針は共通の土台とする(ChatGPT提案の設計ルールを採用)。

### 設計上の絶対ルール
- 商品名をキーにしない(商品IDで管理する)
- 内容量を文字列で保存しない(数値と単位を分離。例:`900` + `g`)
- レシート単位でしか記録しない設計にしない(商品単位で記録する)
- 履歴を編集できない設計にしない

### テーブル構成

**Product(商品マスター)**
- 商品ID / 商品名 / カテゴリ / 内容量(数値) / 単位 / JANコード / 重量 / メモ / お気に入り / ゾーン順(売り場動線)

**Purchase(購入履歴)**
- 商品ID / 購入価格 / 数量 / 購入日 / 割引額 / 店舗 / ShoppingTripID
- ※価格履歴用の別テーブルは作らず、Purchaseから価格推移を計算する

**ShoppingTrip(買い物単位)**
- 日付 / 予算 / 実際の購入額 / メモ

**ProductAlias(商品エイリアス)**
- alias_id / alias_name(自宅で入力した仮の商品名。例:「トイペ」) / product_id(紐付け先の商品ID)
- 表記ゆれを吸収しつつ、内部的には商品IDで一元管理するための仕組み

**Wishlist(事前買い物予定リスト)**
- wishlist_id / 自由入力の商品名(仮) / product_id(紐付け済みなら) / 追加日

**Store(店舗)**
- store_id / 店舗名(例:川崎倉庫店) / 地域
- 複数のコストコ店舗を利用する場合に、店舗ごとの価格差を追跡できるようにするため。Purchaseに store_id を持たせる

**FamilyMember(家族メンバー)**
- member_id / 名前 / モード設定(シンプル/パワーユーザー)
- 「誰が迷いログを残したか」「家族内ランキング」機能の土台。ShoppingTrip・迷いログにmember_idを持たせる

**UnitMaster(単位マスター)**
- unit_id / 単位名(g, ml, 個, パック等) / 換算基準
- 単位の表記ゆれや換算ミスを防ぐための固定マスター

**schema_version(スキーマバージョン管理)**
- version / 適用日
- 将来テーブル構造を変更する際に、過去データが壊れないよう移行(マイグレーション)処理を安全に行うための土台。非エンジニアが後から構造を変える事故を防ぐ

**インデックス方針**
- Purchase(product_id, purchase_date)に索引を張り、価格推移や頻度計算を高速化
- Wishlist(trip_id)に索引を張り、買い物単位での一覧表示を高速化

### 事前リスト機能と商品マスターの整合性について

自宅で作る「買うものリスト」は、実際の商品マスター(Product)と最初から完全一致させようとしない。以下の2段階方式で整合性の問題を解決する。

1. **自宅では自由入力でOK**:「トイレットペーパー」とだけ入力すればよく、内容量や正確な商品名の入力は不要。過去の登録があれば予測変換で候補が出る
2. **店内でカートに入れる瞬間に商品マスターと紐付け**:Wishlist項目をタップすると、対応するProductを選択する画面が出る。過去に紐付けた実績があれば自動で候補が出る
3. **一度紐付けたら別名として記憶(ProductAlias)**:次回から同じ仮名称で入力すれば、候補トップに出るようになり、使うほど精度が上がる
4. **内容量・価格が前回と違えば都度確認して更新**:g単価比較の正確性を常に保つ

この仕組みにより、「商品名をキーにしない」という設計ルールを守りながら、自宅での気軽な入力と店内での正確なデータ管理を両立できる。

---

## 6. UI/UXコンセプト

最優先する4原則:
1. **片手操作**:カートを押しながらでも操作できる。主要ボタンは画面下部(親指の可動域)に集約
2. **視線移動の最小化**:合計金額・予算進捗は常時大きく固定表示
3. **入力ゼロに近い操作**:文字入力よりタップ・スキャン・音声を優先
4. **誤操作からの回復のしやすさ**:特に「削除」系の操作には必ずUndo(元に戻す)通知を出し、店内での焦った操作によるデータロスを防ぐ

**迷いログの入力コストを最小化する**
差別化機能である迷いログも、入力が面倒だと結局使われなくなる。「買った/迷った」のワンタップ選択+理由は事前定義した選択肢からのタップのみとし、最大2タップで記録が完結する設計にする。

**入力を減らす3段階のフォールバック構造**
1. 「マイ定番棚」から過去購入商品をタップして追加(入力なし)
2. 足りない分はバーコードスキャンで補完
3. 未知の商品だけ音声入力または写真撮影で記録し、詳細は帰宅後に整える

---

## 7. 機能一覧(優先度別・v2で真のMVPをさらに絞り込み)

### フェーズ1-a:真の最小ループ(2週間以内の完成を目指す)
これだけで実際に店内で使える最小状態とする。ここが動かなければ他の機能に意味がない。
- カート内商品のリアルタイム合計金額表示
- 予算設定と進捗のプログレスバー表示
- マイ定番棚(よく買う商品のワンタップ追加)
- 片手操作前提のUI(大きな+/-ボタン、下部集約レイアウト)

### フェーズ1-b:MVPの拡充
1-aが実店舗で問題なく動くことを確認してから着手する。
- 商品の内容量(g/ml)ベースの単価自動計算
- 過去購入履歴・購入頻度の確認
- 過去価格との比較(値上がり/値下がり表示)
- 事前買い物予定リスト(自宅で自由入力→店内で商品マスターと紐付け)

### フェーズ1-c:自動リスト生成(アプリの核・優先実装)
1-bまででPurchase履歴がある程度貯まった段階で着手する。このアプリの最も重要な差別化機能と位置づけ、フェーズ2以降の周辺機能より先に実装することを推奨する。
- 購入履歴から商品ごとの平均購入周期を計算する
- 次回のトリップが近づいたら、購入周期に基づいてWishlistに候補を自動で並べる
- ユーザーは「不要なものを外す」「足りないものを足す」だけでリストが完成する
- **注意(コールドスタート問題)**:この機能は一定期間・複数回の購入データが蓄積されるまで精度が出ない。データが少ない初期は「まだ十分な履歴がありません」と表示し、無理に不正確な予測を出さない設計にする

### フェーズ2:買い物体験を磨く機能
- バーコードスキャンでの商品登録
- 音声入力でのハンズフリー登録
- 店内動線に沿ったゾーン別ソート(日用品→デリカ→ベーカリー→精肉→冷凍冷蔵)
- レシートOCRでの購入履歴自動反映
- オフライン完全対応(PWA/ローカルDB)
- 保冷バッグチェック(要冷蔵・冷凍商品の抽出)
- 家族間のリアルタイムカート共有

### フェーズ3:このアプリならではの独自機能
- 迷いログ(買った/迷った+理由の記録、次回来店時に想起表示)— たまに初めての商品で迷う場面向けの補助機能。アプリの核は自動リスト生成(フェーズ1-c)に移行したため、こちらは優先度を下げてよい
- 買わなかった履歴
- 買い物終了チェック(見落とした売り場の確認)
- 価格信頼度(★評価で価格変動の激しさを表示)
- 次回購入候補の自動予測(購入周期から)
- 次回コストコまで在庫が持つかの予測表示
- シェア(割り勘)単価計算
- フードコート予算キープ

### フェーズ4:遊び心・継続利用の仕掛け
- 戦利品画面(合計金額・商品数・総重量をゲーム風に表示)
- コストコあるあるコメント表示(「今回もコストコの魔法にかかりました」等)
- 我が家の定番認定(10回→殿堂入り、30回→定番、50回→伝説)
- コストコ偏差値診断(食品/日用品/衝動買いの割合)
- 年間利用レポート
- 家族内ランキング
- 思い出イベントタグ(BBQ、クリスマス等)
- コストコタイムライン(年表表示)
- 今回はいつもと違う表示(前回との差分をハイライト)
- オープニングクロール演出(買うものリストがスターウォーズ風に流れて表示される。CSS 3D perspectiveで実装可。台詞・フォントは著作権配慮でオリジナルにアレンジ。初回起動時のみ/設定でON-OFF可能/または戦利品画面での使用を想定。※現時点では未着手・保留)

---

## 8. リスクと対策(非エンジニア個人開発でありがちな落とし穴)

- **機能を増やしすぎて完成しない**:フェーズ1-aに明確な期限(2週間)を設け、まず「動く最小限」を実現することを最優先する
- **データ構造を後から変えて過去データが壊れる**:schema_versionテーブルを最初から用意し、将来の構造変更を安全に行える土台を持っておく
- **家族が結局使ってくれない**:シンプルモードで入力負担を最小化し、初期は開発者本人だけが入力担当でも成立する設計にしておく。家族の参加は急がない
- **燃え尽きて途中で放置される**:一度に完璧を目指さず、フェーズごとに「完成させて実際に使う」という小さな達成体験を積み重ねる。フェーズ1-aが動いた時点を一つの区切りとして扱う

## 9. 成功指標(このアプリが機能しているかの確認軸)

- フェーズ1-a完成から3ヶ月後も、実際にコストコで使い続けられているか
- レジでの会計時に「想定より高かった」という驚きが減っているか(予算進捗表示の効果測定)
- 迷いログが実際に次回の意思決定に役立っているか(「前回も見送った」を見て判断が楽になったという実感があるか)

これらは数値で厳密に測るものではなく、「作って満足」で終わらせず実際の店内での使用実感を継続的に振り返るための確認軸として位置づける。

## 10. アプリ名の候補(遊び心・愛着形成のために)

「仮」のままだと愛着が持ちにくいため、核となる自動リスト生成のニュアンスを踏まえた候補をいくつか挙げる。
- **コストコ相棒**:シンプルに「一緒に買い物する存在」というポジショニング
- **手ぶらコストコ**:リストを自分で作らなくていい、という自動生成の価値をそのまま名前に
- **コストコ手帖**:記録・蓄積していくアプリという性格を表現
- **いつものコストコ**:ルーティン購入を前提にした親しみやすさを表現

正式名称は、フェーズ1-aを実際に使ってみて、アプリの"人格"がもう少し見えてきた段階で決めるのでも遅くない。

---

## 11. 開発の進め方(手順)

1. **開発環境の準備**:GitHubリポジトリ作成、React(Vite)+TypeScriptのプロジェクト初期化、GitHub Pagesでのホスティング設定
2. **データ基盤の最小構築**:Product / Purchase / ShoppingTripと、Web Worker上でのsql.js動作環境を用意(この段階ではProductAlias等は後回しでよい)
3. **フェーズ1-aの実装(目安:2週間以内)**:合計金額・予算進捗・マイ定番棚・片手UIのみに絞って完成させる
4. **実店舗での初回テスト**:フェーズ1-aだけを実際に持って行って使ってみる。ここで初めて「本当に使えるか」が分かる
5. **フェーズ1-bの実装**:単価比較・履歴・事前リストなど、MVPを拡充する
6. **フェーズ1-cの実装(アプリの核)**:購入データが貯まった段階で、自動リスト生成に着手する。他のフェーズ2・3の機能より優先する
7. **フェーズ2の実装**:買い物体験(スキャン・音声・ゾーンソート等)を磨く
8. **フェーズ3の実装**:迷いログなど補助的な独自機能を追加する
9. **フェーズ4の実装**:遊び心要素を追加し、家族が継続して使いたくなる仕掛けを加える

各フェーズの完了ごとに、実際に家族で使ってみてフィードバックを反映してから次のフェーズに進む進め方を推奨する。特にフェーズ1-aは「完成させて使う」という成功体験を早期に得ることが、個人開発を継続させる上で最も重要なステップと位置づける。
