# 我が家専用コストコ買い物リストアプリ 開発コンセプト v3

2026年8月、STEP0(開発環境)とFirebase基盤を残してアプリを白紙化し、SOTAさんとゼロベースで
話し合った内容をまとめたもの。`costco_app_concept_v2.md`(初期企画書)を置き換える最新版。
v2との違いは「6. v2からの主な変更点」を参照。

このドキュメントはあくまで**方向性の合意メモ**であり、細部の仕様(データ構造・画面遷移の
詳細など)は実装に着手する段階で改めて詰める。「4. 未確定・今後決めること」に、まだ相談が
必要な点をまとめてある。

---

## 1. 一枚要約

コストコ店内でカートを押しながら片手で使う、我が家専用の買い物アプリ。
オブジェクト指向UI(OOUI: 「今から何をする」という動詞よりも先に、「カート」「商品」
「買うものリスト」といった対象(オブジェクト)そのものを画面の主役に置く設計思想)を意識し、
買い物中の画面では**カートそのものを大きく主役に据える**。事前に立てた計画と、店内での
とっさの追加(バーコードスキャン)の両方を、同じ「カート」というオブジェクトに集約する。

---

## 2. 画面構成

大きく2つの画面と、カートの詳細画面で構成する。

### 画面A:事前計画画面(買い物に行く前に使う)

- 「定番商品リスト」(毎回繰り返し買う商品の登録リスト。旧アプリのマイ定番棚に近い)から、
  今回買うものを取捨選択して「今回買うものリスト」を作る
- 定番商品リストへの新規登録時は、商品名・価格の入力補助として、以前アップロードした
  商品名候補データベース(`src/data/productCatalog.ts`、costcotuu.com由来・約3200件)と連携する
- 予算を設定し、選択中の商品の概算合計金額をリアルタイムに表示する(旧アプリの
  「見込み合計金額」に近い)。**予算は旧アプリと同様「買い物1回ごと」の単位で管理する**
  (月間予算などの期間管理は今回は持たせない)
- 定番商品リスト・今回買うものリストは、どちらもカテゴリ別に区分けして表示する

### 画面B:買い物中のカート中心画面(コストコ店内で使う)

**「カートそのものに集中する」画面。** 計画リストの一覧などは表示せず、以下の3要素のみで構成する。

- 画面中央に大きなカートのイラスト(またはアイコン)
- カートの上部に、ワンタップでバーコードスキャンを起動するカメラアイコン
- カートの下部に、カート内商品の合計金額

**商品の追加方法は2通り。**

1. 計画リスト(今回買うものリスト)にある商品:現物を実際のカートに入れたタイミングで、
   ワンタップでアプリのカートにも反映させる
2. 計画リストにない商品(店内で急遽欲しくなったもの):カメラアイコンからバーコードを
   スキャンし、商品名・価格・内容量の入力を補助する(バーコードから自動で情報が
   引けない場合は手入力にフォールバックする。技術面の注記は「4. 未確定・今後決めること」参照)

商品がカートに追加されるたびに、カートのイラストの中身が連動して増えていくアニメーションを
付ける。**点数に上限は設けず、カートが山盛りになる(いっぱいに見える)まで増え続ける表現に
する。** 大量購入時にどこかで数字表示に切り替える、といった打ち切り方はしない。

**プロトタイプ検討を経て、見た目の仕様は次の通り決定した**(チャット上での試作を重ねて
SOTAさんと確認済み):
- カートのイラスト本体は、SOTAさん提供の実際の写真(`src/assets/cart-icon.jpg`)をそのまま使う
- カートの中に入る「商品」の表現は、商品ごとのオリジナルイラストを描き起こすのではなく、
  **絵文字(🥛🧃📦🍞🧀🥩🥦🍊🥫🥚🍫🧻など、コストコで買う食品・日用品に近いもの)を
  ランダムに選んで使う**。OS標準搭載のため追加のダウンロード・ライセンス費用が一切
  発生せず、iPhone(Apple Color Emoji)では特に艶やかでカラフルな見た目になる
  (Twemoji等の外部素材を使う案も検討したが、まずは絵文字で試す)
- 商品(絵文字)は、大きさを1.5〜2倍の範囲でランダムに変え、10〜20%程度ランダムに
  重ねて配置し、傾きも1つ1つランダムに付けることで、雑多に詰め込まれた自然な見た目にする
  (全部同じ大きさ・向きだと不自然に見えることを確認済み)
  - カートの手前(写真の左側)まで隙間なく敷き詰め、カート枠の内側にきちんと収まって
    見えるようにする(実装時、カートの写真のどの範囲が「商品を入れてよい領域」かを
    ある程度正確に把握しておく必要がある)
  - 商品が増えて基本の領域を超えたら、カートの縁を越えて上に山盛りになっていく

**画面最下部には「買い物を終了する」ボタンを配置する。** タップすると今回の買い物が
終了したものとみなし、カートの中身を購入履歴に反映する。誤ってボタンを押してしまった
場合に備え、確定後も購入履歴側から内容を修正できるようにする(下の「3. 履歴・比較機能」の
購入記録の訂正・削除と合わせて対応する)。

### カートの詳細画面(カートをタップすると開く)

- 現在カートに入っている商品の一覧・編集・削除
- 今回買うものリストのうち、まだカートに追加していない商品を
  「未追加◯点:商品名」という形でアラート表示し、そこからワンタップで追加もできる
  (買い忘れ防止。旧アプリの「未購入」バッジに近い役割をここに集約する)
- カートの中身もカテゴリ別に区分け表示する

### 画面C:購入履歴・レポート画面(いつでも振り返れる画面)

買い物の前後に関わらず、いつでも過去の記録を振り返れる画面。**入り口は1箇所に絞らず、
設定画面とカート詳細画面など複数箇所からアクセスできるようにする。**

- 過去の商品購入履歴の一覧(商品ごとに絞り込んで見られるものと、全体を通しで見られるものの
  両方を想定)
- コストコでの買い物1回ごとの合計金額の推移(グラフなどで時系列に見える化する)
- 商品単価の変動推移(値上がり/値下がりの推移を時系列で見える化する)
- 年間利用額(1年間の合計購入金額)

---

## 3. 履歴・比較機能

事前計画画面・カート詳細画面・画面Cのどこからも参照できる、商品単位・買い物単位の振り返り機能。

- **商品単価の比較**:内容量あたりの単価で、前回購入時との値上がり/値下がりを表示する
  (パッケージサイズが変わっても正しく比較できるようにする、という旧アプリの設計方針を踏襲)
- **購入履歴・購入頻度**:商品ごとの過去の購入日一覧、平均購入間隔を表示する
- **買い物1回ごとの合計金額の比較**:今回の合計金額を、過去の買い物(直近や平均)と比べて
  「前回より高い/安い」が分かるようにする(商品単価の比較とは別の切り口として追加)
- **購入記録の訂正・削除**:「買い物を終了する」ボタンの押し間違いなど、確定後に内容が
  違っていた場合に備え、購入履歴の中の1件ずつを直接訂正・削除できるようにする
  (旧アプリで作っていた仕組みを踏襲する想定)
- **推移のグラフ表示・年間利用額**(画面Cの内容):買い物ごとの合計金額の推移、商品単価の
  変動推移をグラフで見える化し、年間の合計利用額も確認できるようにする。**グラフ描画には
  Chart.jsなどの外部ライブラリを使わず、インラインSVGを自前で組み立てて描く**
  (JavaScriptでデータの最小値・最大値から描画領域の座標(x, y)を計算し、
  `<circle>`/`<path>`/`<rect>`/`<line>`/`<text>`などのSVGタグを組み立てて表示する)。
  追加ライブラリが不要になる分、アプリの容量を増やさずに済む

---

## 4. 未確定・今後決めること

実装に入る前に、SOTAさんと相談しながら決めていく点。

- **バーコードスキャンの実現方法**:カメラでバーコードを読み取ること自体は無料のブラウザ機能
  (カメラ利用+スキャン用のJSライブラリ)で実現できる。ただし2026年時点でもiPhoneのSafariは
  ブラウザ標準のバーコード読み取り機能(BarcodeDetector API)に対応しておらず、無料のJS
  ライブラリを追加して実現する方針(具体的なライブラリ名は実装時に選定する)。読み取った
  番号から商品名・価格を自動で引ける無料の商品データベースは、コストコの商品(特に
  カークランドなどのプライベートブランド)だと網羅されていないことが多いため、
  「読み取れたら自動入力、ダメなら手入力」という前提で設計する。**読み取れない商品が
  一定数出ることは前提として許容する**(SOTAさんと確認済み)
- **データの持ち方**:Firestoreにどんなコレクション(定番商品・今回買うものリスト・カート・
  購入履歴など)を置くか、旧アプリの設計をどこまで踏襲しどこを変えるかは、実装に入る
  段階で改めて設計する

---

## 5. 技術方針(白紙化後、変更なしの前提)

- 開発環境・ホスティングはSTEP0のまま(React (Vite) + TypeScript、Tailwind CSS、Lucide、
  GitHub Pages、GitHub Actions自動デプロイ)
- データ保存・認証はフェーズ2のFirebase基盤のまま(Firestore、匿名認証+家族コード方式)。
  詳細は `CLAUDE.md` の「Firebase基盤」節を参照
- **新しいデータ(定番商品リスト・今回買うものリスト・カートの中身・購入履歴など)は
  すべてパートナーとリアルタイム共有する**方針で進める
- 無料運用が絶対条件である点は変更なし。バーコードスキャンなど新しい機能を追加する際も、
  有料サービス・従量課金が発生する可能性があれば必ず事前に明示する

---

## 6. v2からの主な変更点

- v2では「自動リスト生成」(購入履歴と周期から次回のリストを自動で下書きする)を核となる
  差別化機能としていたが、v3では一旦スコープから外している。まずは今回合意した内容
  (カート中心のOOUIデザイン、バーコードスキャン、履歴・比較機能)を形にすることを優先し、
  自動リスト生成は必要になった時点で改めて検討する
- v2では「サーバーを極力使わない・オフラインファースト」の方針でFirebaseを不採用として
  いたが、開発が進む中で「パートナーと共有したい」という要望が生まれ、フェーズ2で
  Firebaseへ全面移行した。v3はその状態を引き継ぐ
- v2の「シンプルモード/パワーユーザーモード」「複数店舗対応」などは、v3の会話では
  改めて話題に出ていないため、現時点ではスコープ外として扱う(必要になれば復活可能)
